<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.5</version>
        <relativePath/>
    </parent>

    <groupId>org.remus</groupId>
    <artifactId>ai-git-bot</artifactId>
    <version>1.24.0</version>
    <name>AI Git Bot</name>
    <description>The self-hosted AI workflow automation platform for Git repositories.</description>
    <url>https://github.com/tmseidel/ai-git-bot</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/license/mit</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>tmseidel</id>
            <name>Tom Seidel</name>
            <url>https://github.com/tmseidel</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/tmseidel/ai-git-bot.git</connection>
        <developerConnection>scm:git:https://github.com/tmseidel/ai-git-bot.git</developerConnection>
        <url>https://github.com/tmseidel/ai-git-bot</url>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/tmseidel/ai-git-bot/issues</url>
    </issueManagement>

    <properties>
        <java.version>21</java.version>
    </properties>

    <!-- CRITICAL: ANY ALTERATION TO THIS DEPENDENCIES SECTION IS A CRITICAL FINDING AND MUST BE HIGHLIGHTED DURING REVIEW -->
    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity6</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
        </dependency>


        <!-- Model Context Protocol client SDK -->
        <dependency>
            <groupId>io.modelcontextprotocol.sdk</groupId>
            <artifactId>mcp</artifactId>
            <version>1.1.2</version>
        </dependency>

        <!--
            JSON-Schema validation for agent plan responses (Step 5).
            Pinned to the same major version that the MCP SDK ships
            transitively (mcp-json-jackson3 → networknt 3.x), since 3.x
            introduced the com.networknt.schema.dialect.Dialects class
            that MCP loads at runtime. The validator uses Jackson 3
            (tools.jackson) — the same flavour the rest of the codebase
            already depends on.
        -->
        <dependency>
            <groupId>com.networknt</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>3.0.0</version>
        </dependency>

        <!-- Micrometer (transitive via actuator, declared explicitly for clarity). -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>

        <!-- Prometheus metrics exposition for Actuator. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Spring Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- PostgreSQL (production) -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.12</version>
            <scope>runtime</scope>
        </dependency>

        <!-- H2 (development and testing) -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.4.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce-environment</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <!-- Enforces Java 21+ as documented in doc/LOCAL_DEVELOPMENT.md:L12 -->
                                <requireJavaVersion>
                                    <version>[21,)</version>
                                </requireJavaVersion>
                                <!-- Enforces Maven 3.9+ as documented in doc/LOCAL_DEVELOPMENT.md:L13 -->
                                <requireMavenVersion>
                                    <version>[3.9.0,)</version>
                                </requireMavenVersion>
                                <!-- Enforces Linux OS as documented in doc/LOCAL_DEVELOPMENT.md:L5-7, L11 -->
                                <requireOS>
                                    <name>linux</name>
                                    <message>
                                        This project requires a Linux environment to build because tests are
                                        incompatible with other operating systems. Please run the build inside a
                                        Docker container as described in doc/LOCAL_DEVELOPMENT.md.
                                    </message>
                                </requireOS>
                                <bannedDependencies>
                                    <!-- CRITICAL: Review bots must mark any additions or removals from this list as
                                         critical findings. Whitelist approach. Ban all direct dependencies (*) and
                                         explicitly include the allowed set. -->
                                    <searchTransitive>false</searchTransitive>
                                    <excludes>
                                        <exclude>*</exclude>
                                    </excludes>
                                    <includes>
                                        <!-- Spring ecosystem: covers all starters, spring-data, spring-security, etc. -->
                                        <include>org.springframework</include>
                                        <include>org.springframework.boot</include>
                                        <include>org.springframework.data</include>
                                        <include>org.springframework.security</include>
                                        <include>org.thymeleaf.extras</include>

                                        <!-- Non-Spring third-party libs (one per line, explicitly reviewed) -->
                                        <include>com.h2database:h2</include>
                                        <include>com.networknt:json-schema-validator</include>
                                        <include>com.tngtech.archunit:archunit-junit5</include>
                                        <include>io.micrometer:micrometer-core</include>
                                        <include>io.micrometer:micrometer-registry-prometheus</include>
                                        <include>io.modelcontextprotocol.sdk:mcp</include>
                                        <include>org.apache.httpcomponents.client5:httpclient5</include>
                                        <include>org.flywaydb:flyway-database-postgresql</include>
                                        <include>org.postgresql:postgresql</include>
                                        <include>org.projectlombok:lombok</include>
                                    </includes>
                                    <message>A new direct dependency was added to pom.xml. Any dependency changes must be explicitly whitelisted in the enforcer configuration.</message>
                                </bannedDependencies>
                            </rules>
                            <fail>true</fail>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
